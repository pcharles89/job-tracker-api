jwt:
  secret: my-super-secret-key-that-is-at-least-32-characters-long
  expiration: 86400000
spring:
  datasource:
    url: jdbc:h2:mem:jobtracker_test
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.H2Dialect
  flyway:
    enabled: false