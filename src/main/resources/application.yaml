jwt:
  secret: ${JWT_SECRET}
  expiration: ${JWT_EXPIRATION:86400000}
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jobtracker
    username: root
    password: password
  jpa:
    hibernate:
      ddl-auto: validate

